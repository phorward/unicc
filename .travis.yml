language: C
sudo: enabled
script: ./.travis.sh

before_install:
  - wget https://github.com/phorward/phorward/archive/v0.21.0.tar.gz
  - tar xvfz v0.21.0.tar.gz
  - pushd phorward-0.21.0 && ./configure --prefix=/usr && make && sudo make install && popd
  - touch *
 
